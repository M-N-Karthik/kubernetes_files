apiVersion: v1
kind: Service
metadata:
  name: colour-api-nodeport
  labels:
    app: colour-api
spec:
  type: NodePort
  selector:
    app: colour-api
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
